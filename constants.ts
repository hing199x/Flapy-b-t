export const GAME_WIDTH = 400; // Mobile-first width style inside the container
export const GAME_HEIGHT = 600;
export const BIRD_SIZE = 30;
export const PIPE_WIDTH = 50;
export const GROUND_HEIGHT = 50;

// Physics
export const GRAVITY = 0.6;
export const JUMP_STRENGTH = -8;
export const PIPE_SPEED = 3;
export const PIPE_SPAWN_RATE = 100; // Frames between pipes
export const GAP_SIZE = 160;

// Limits
export const MAX_Y = GAME_HEIGHT - GROUND_HEIGHT - BIRD_SIZE;
export const MIN_Y = 0;
